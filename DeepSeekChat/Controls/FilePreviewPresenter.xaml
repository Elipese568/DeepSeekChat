<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DeepSeekChat.Controls.FilePreviewPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeepSeekChat.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:tkcont="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Grid x:Name="PART_FilePreviewArea" Padding="36,9,36,40" RowSpacing="9" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="ExitPreviewFile" Click="ExitPreviewFile_Click" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Style="{ThemeResource SubtleButtonStyle}" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="1,2,0,0">
                <FontIcon Glyph="&#xE711;" />
                <TextBlock x:Uid="ms-resource:///DiscussionPage/ExitButtonTextBlock"/>
            </StackPanel>
        </Button>
        <tkcont:Segmented x:Name="ContentSelection" Grid.Row="0" SelectedIndex="0" HorizontalAlignment="Right" VerticalAlignment="Top">
            <tkcont:SegmentedItem Tag="RenderedContent">
                <tkcont:SegmentedItem.Icon>
                    <FontIcon Glyph="&#xE8FF;" />
                </tkcont:SegmentedItem.Icon>
                <TextBlock x:Uid="ms-resource:///DiscussionPage/AnalyzedResultItemContent"/>
            </tkcont:SegmentedItem>
            <tkcont:SegmentedItem Tag="RawData">
                <tkcont:SegmentedItem.Icon>
                    <FontIcon Glyph="&#xE7C3;" />
                </tkcont:SegmentedItem.Icon>
                <TextBlock x:Uid="ms-resource:///DiscussionPage/RawDataItemContent"/>
            </tkcont:SegmentedItem>
        </tkcont:Segmented>
        <Grid x:Name="PART_FilePreviewer" Grid.Row="1" RowSpacing="36" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-36,0,-36,-40" Padding="36,40" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Style="{ThemeResource TitleTextBlockStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Text="{x:Bind PreviewFileViewModel.Name,Mode=OneWay}"/>
            <tkcont:SwitchPresenter HorizontalAlignment="Stretch" Grid.Column="0" VerticalAlignment="Stretch" Grid.Row="1" Value="{Binding SelectedItem.Tag,ElementName=ContentSelection}">
                <tkcont:Case Value="RenderedContent">
                    <TextBox AcceptsReturn="True" TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{x:Bind PreviewFileViewModel.Content,Mode=TwoWay}"/>
                </tkcont:Case>
                <tkcont:Case Value="RawData">
                    <tkcont:SwitchPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Value="{x:Bind AsString(PreviewFileViewModel.Type),Mode=OneWay}">
                        <tkcont:Case Value="Document">
                            <ScrollViewer HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" VerticalScrollMode="Enabled">
                                <TextBlock TextWrapping="Wrap" Text="{x:Bind GetRawDataString(PreviewFileViewModel.InnerObject.CopiedFileUri),Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </ScrollViewer>
                        </tkcont:Case>
                        <tkcont:Case Value="Media">
                            <Image Source="{x:Bind GetRawImageSource(PreviewFileViewModel.InnerObject.CopiedFileUri),Mode=OneWay}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        </tkcont:Case>
                    </tkcont:SwitchPresenter>
                </tkcont:Case>
            </tkcont:SwitchPresenter>
        </Grid>
    </Grid>
</UserControl>
