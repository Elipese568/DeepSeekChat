<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DeepSeekChat.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeepSeekChat"
    xmlns:v="using:DeepSeekChat.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DeepSeekChat">
    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>
    <Grid Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar Title="DeepSeekChat"  Subtitle="Preview" x:Name="MyTitleBar" IsBackButtonVisible="True">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/AppIconLight.png"/>
            </TitleBar.IconSource>
        </TitleBar>
        <Frame Grid.Row="1" x:Name="ContentFrame" SourcePageType="v:MainPage"/>
    </Grid>
    
</Window>
